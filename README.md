# Submission for Aknamed

Steps:

1. Clone the repository.
2. Use `yarn storybook`

The component being displayed is present in `src/components`. The two components are `Input.js` and `SingleSelect.js`.